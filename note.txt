Server Side Rendering (SSR)

it Updates frequently, it used mostly when you need real time data on your page. just like ssg that is time bound you can set the revalidation time to 0.

Ppr partial prerendering method it combines static and dynamic rendering.


Nextjs Backend routing: API ROUTE CREATION

for you to create a backend route is super simple in nextjs create a special folder like api, then create a route.ts inside it and from there you can immmediately begin writing your server code.
export async function GET() and return response.json.
Then how do you create other api endpoints like Post, put, patch and delete. for that lets create a book  endpoint with a local array as a data base.

for delete endpoint you have to create a dynamic route handler

